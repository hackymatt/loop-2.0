"use client";

// ----------------------------------------------------------------------

export function LearnView({
  course,
  chapter,
  lesson,
}: {
  course: string;
  chapter: string;
  lesson: string;
}) {
  console.log(course, chapter, lesson);
  return <>abc</>;
}
